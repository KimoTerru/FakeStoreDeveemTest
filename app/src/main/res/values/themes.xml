<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FakeStoreDeveemTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/gray_average</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="Theme.FakeStoreDeveemTest" parent="Base.Theme.FakeStoreDeveemTest" />
</resources>